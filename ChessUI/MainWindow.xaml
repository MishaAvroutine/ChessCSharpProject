<Window x:Class="ChessUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ChessUI"
        mc:Ignorable="d"
        Title="Chess" Width="700" Height="650"
        Icon="Assets/icon.ico"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource FillColor}"
        KeyDown="Window_KeyDown">

    <Border Style="{StaticResource BorderStyle}">
        <Grid Background="{StaticResource StrokeColor}">
            <Grid.ColumnDefinitions>
                <!-- Sidebar column -->
                <ColumnDefinition Width="120"/>
                <!-- Board column -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Sidebar on the left -->
            <StackPanel Grid.Column="0" Grid.Row="0" Grid.RowSpan="2" Background="{StaticResource FillColor}" Margin="0">
                <TextBlock Text="Moves" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                <ListBox x:Name="MoveListBox" Height="300" Style="{StaticResource MoveListBoxStyle}"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                    <Button Content="←" Width="45" Height="25" Margin="3" Style="{StaticResource ButtonStyle}" Click="PrevButton_Click"/>
                    <Button Content="→" Width="45" Height="25" Margin="3" Style="{StaticResource ButtonStyle}" Click="NextButton_Click"/>
                </StackPanel>
            </StackPanel>
            
            <!--AI TOGGLE-->
            <Button x:Name="AIbutton" Grid.Row="1" Content="AI: OFF" Width="60" Height="25" Margin="3" Style="{StaticResource ButtonStyle}" Click="AIButton_Click"/>
            
            <!-- Chess Board in second column -->
            <Viewbox Grid.Column="1" Margin="0" Grid.Row="0" Grid.RowSpan="2">
                <Border Width="500" Height="623" Style="{StaticResource BorderStyle}">
                    <StackPanel>

                        <!-- Black Side Timer -->
                        <Grid Margin="0,10,0,10" Height="40">
                            <TextBlock x:Name="BlackTimerTextBlock"
                                       Text="05:00"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="DarkSlateGray"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Grid>

                        <!-- Board with coordinates -->
                        <Grid Width="500" Height="500">

                            <Grid.ColumnDefinitions>
                                <!-- Left margin for rank numbers -->
                                <ColumnDefinition Width="20"/>
                                <!-- Actual board -->
                                <ColumnDefinition Width="*"/>
                                <!-- Right margin (optional) -->
                                <ColumnDefinition Width="20"/>
                            </Grid.ColumnDefinitions>

                            <Grid.RowDefinitions>
                                <!-- Top margin for file letters -->
                                <RowDefinition Height="20"/>
                                <!-- Actual board -->
                                <RowDefinition Height="*"/>
                                <!-- Bottom margin for file letters -->
                                <RowDefinition Height="20"/>
                            </Grid.RowDefinitions>

                            <!-- The 8x8 board goes in center -->
                            <Grid x:Name="BoardGrid"
          Grid.Row="1" Grid.Column="1"
          MouseDown="BoardGrid_MouseDown">
                                <Grid.Background>
                                    <ImageBrush x:Name="BoardImage" ImageSource="/Assets/Board.png" 
                        RenderOptions.BitmapScalingMode="HighQuality"
                        RenderOptions.EdgeMode="Aliased"/>
                                </Grid.Background>

                                <UniformGrid x:Name="HighLightGrid" Rows="8" Columns="8"/>
                                <UniformGrid x:Name="PieceGird" Rows="8" Columns="8"
                     RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Grid>

                            <!-- Rank numbers (left side) -->
                            <UniformGrid Grid.Row="1" Grid.Column="0" Rows="8" Columns="1" VerticalAlignment="Stretch" HorizontalAlignment="Center">
                                <TextBlock Text="8" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="7" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="6" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="5" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="4" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="3" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </UniformGrid>

                            <!-- File letters (bottom side) -->
                            <UniformGrid Grid.Row="2" Grid.Column="1" Rows="1" Columns="8" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                                <TextBlock Text="a" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="b" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="c" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="d" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="e" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="f" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="g" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                <TextBlock Text="h" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </UniformGrid>
                        </Grid>

                        <!-- White Side Timer -->
                        <Grid Margin="0,10,0,10" Height="40">
                            <TextBlock x:Name="WhiteTimerTextBlock"
                                       Text="05:00"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Foreground="DarkSlateGray"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center"/>
                        </Grid>

                    </StackPanel>
                </Border>
            </Viewbox>
            <ContentControl Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="0" Grid.RowSpan="2" x:Name="MenuContainer" Margin="0,-24,0,24"/>
        </Grid>
    </Border>
</Window>
